# Estrcutura página principal

A deps rumext + react, main.js cargando, (init) monta root
B layout* vacío con grid y variables CSS Penpot-like
C app.main.api con 3 fetch (copia fiel del JS)
D character-list* funcionando
E click en li → character-detail* en panel
F persona-form* + resultado en panel
G borrar script inline

┌──────────────────┬─────────────────────────────┐
│  SIDEBAR         │  MAIN PANEL                 │
│  · lista chars   │  · avatar, intro, CV        │
│  · form persona  │  · mensaje saludar-persona  │
│  · status bar    │                             │
└──────────────────┴─────────────────────────────┘